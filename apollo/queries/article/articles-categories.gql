query Category($id: ID!) {
  category(id: $id) {
    name
    slug
    articles {
      id
      title
      content
      image {
        url
      }
      categories {
        id
        name
      }
    }
  }
}
